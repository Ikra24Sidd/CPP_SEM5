#include <bits/stdc++.h>
using namespace std;
int main()
{
	string str;
	getline(cin, str);
	int maxLen = 0, len, sIdx, eIdx, prevIdx = -1;
	for (int i = 0; i < str.length(); i++)
	{}
	// for (int i = 0; i < str.length(); i++)
	// {
	// 	if (str[i] == ' ')
	// 	{
	// 		if (prevIdx == -1)
	// 		{
	// 			len = i - 0;
	// 		}
	// 		else
	// 			len = i - prevIdx;
	// 		maxIdx = prevIdx + 1;
	// 		prevIdx = i;
	// 	}
	// 	if (len > maxLen)
	// 	{
	// 		maxLen = len;
	// 		// maxIdx = prevIdx;
	// 	}
	// }
	// for (int i = maxIdx; i < maxLen; i++)
	// {
	// 	cout << str[i];
	// }

	return 0;
}